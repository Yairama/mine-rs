[package]
name = "marvin-benchmark"
version = "0.1.0"
edition = "2024"

[dependencies]
mine-sdk = { path = "../../crates/mine-sdk" }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
minilp = "0.2"

[lints]
workspace = true
