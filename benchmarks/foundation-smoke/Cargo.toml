[package]
name = "foundation-smoke"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
mine-core = { path = "../../crates/mine-core" }

[dev-dependencies]
criterion = { workspace = true }

[lints]
workspace = true

[[bench]]
name = "foundation_smoke"
harness = false
